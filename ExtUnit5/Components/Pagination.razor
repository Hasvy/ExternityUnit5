@typeparam TItem

<div class="d-flex flex-row justify-content-center">
    <button class="btn btn-outline-primary" type="button" @onclick="@(() => ChangePage(1))" disabled="@_isFirstPage">First</button>
    <button class="btn btn-outline-primary ms-2" type="button" @onclick="@(() => ChangePage(PageNumber - 1))" disabled="@_isFirstPage">Previous</button>

    <div class="list-group list-group-horizontal mx-3">
        @foreach (var pageNum in PageNumbers)
        {
            <button @onclick="@(() => ChangePage(pageNum))" style="min-width: 50px;" class="list-group-item @(pageNum == PageNumber ? "active" : "")">@pageNum</button>
        }
    </div>

    <button class="btn btn-outline-primary" type="button" @onclick="@(() => ChangePage(PageNumber + 1))" disabled="@_isLastPage">Next</button>
    <button class="btn btn-outline-primary ms-2" type="button" @onclick="@(() => ChangePage(TotalPages))" disabled="@_isLastPage">Last</button>
</div>